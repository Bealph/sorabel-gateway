[project]
name = "sorabel-data-gateway"
version = "0.1.0"
description = "Serveur MCP gouverne exposant un RAG avance et du Text-to-SQL lecture seule pour Sorabel."
requires-python = ">=3.11"

# Dependances a valider en phase de developpement (voir CLAUDE.md, section 5).
# dependencies = [
#   "mcp",              # SDK MCP / FastMCP
#   "chromadb",         # store vectoriel : chroma, tranche par D32
#   "rank-bm25",        # recherche lexicale (hybride)
#   "sentence-transformers",  # embeddings + cross-encoder (reranking)
#   "sqlglot",          # parsing / validation SQL (garde-fous lecture seule)
# ]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
