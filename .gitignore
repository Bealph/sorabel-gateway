# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.python-version

# Environnements & secrets
.env
.env.*
!.env.example

# Outils
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Donn̩es locales & runtime
.docker-data/
chroma-data/
logs/*.jsonl
data/sorabel.db
# Index documentaire : artefact GENERE par `python -m ingest`, binaire et
# reconstructible en une minute. Un artefact reconstructible ne se versionne pas.
data/index/

# IDE / OS
.idea/
.vscode/
.DS_Store

# --- Ajouts du depot de conception -------------------------------------------
# Le corpus, lui, EST versionne en amont : ne pas le reexclure.
env/
venv/
.ipynb_checkpoints/
*.key
*.index
*.faiss
.chroma/
eval/results/*.json
eval/results/*.csv
*.log

# On garde les recommandations d'extension, le reste de .vscode est ignore.
!.vscode/
.vscode/*
!.vscode/extensions.json

# Point de chargement local de la memoire de projet. Le contenu versionne est
# dans MEMOIRE_PROJET.md : ce fichier n'en est qu'un import, pour ne pas avoir
# deux copies de la meme verite.
CLAUDE.md
.claude/
